public class Fibonacci {
    public static int fib(int n) {
        if (n <= 1) return n; // Base cases
        return fib(n - 1) + fib(n - 2);
    }

    public static void main(String[] args) {
        System.out.println("5th Fibonacci element: " + fib(5));
    }
}
